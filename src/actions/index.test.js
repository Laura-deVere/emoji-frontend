import moxios from 'moxios';

import { storeFactory } from '../test/testUtils';
import { signUp } from './';

describe('signIn action creator', () => {
    // beforeEach(() => {
    //     moxios.install();
    // });
    // afterEach(() => {
    //     moxios.uninstall();
    // });

    // test('Adds response')
});
